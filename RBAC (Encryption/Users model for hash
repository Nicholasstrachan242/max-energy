from pydal.validators import IS_EMAIL

db.define_table(
    'user',
    Field('username', unique=True),
    Field('password_hash'),  # Store hashed password
    Field('email_encrypted'),  # Store encrypted email
    Field('encryption_iv'),    # Store IV for decryption if needed
    format='%(username)s'
)
